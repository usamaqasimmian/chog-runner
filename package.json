{
  "name": "chog-runner-game",
  "version": "1.0.0",
  "description": "A runner game with leaderboard functionality",
  "main": "index.js",
  "scripts": {
    "dev": "npx vercel dev",
    "build": "echo 'No build step needed'",
    "start": "npx vercel dev",
    "deploy": "npx vercel"
  },
  "dependencies": {
    "@vercel/blob": "^0.20.0",
    "@vercel/kv": "^1.0.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "vercel": "^32.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
